[package]
name = "vellum-lock"
version = "0.1.0"
edition = "2021"

[lib]
path = "mod.rs"

[dependencies]
sqlx = { version = "0.7", features = ["runtime-tokio", "postgres"] }
tokio = { version = "1", features = ["time"] }
